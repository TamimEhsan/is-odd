{
  "name": "is-odd",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "patch-package"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "is-even": "1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "patch-package": "^8.0.0",
    "postinstall-postinstall": "^2.1.0"
  }
}
